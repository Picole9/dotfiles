# fish

Configuration for [fishshell](https://fishshell.com/)

- plugin-manager: [fisher](https://github.com/jorgebucaran/fisher), usage `fisher install { repo }`, `fisher update`
- local fish-config, which is in gitignore: `localconf.fish`
